spring:
  datasource:
    hikari:
      jdbc-url: jdbc:sqlite:memory:myDb?cache=shared
      driver-class-name: org.sqlite.JDBC
      username: sa
      password: sa
  jpa:
    hibernate:
      ddl-auto: create # validate
    show-sql: true
    database-platform: io.github.starrybleu.sideprject0.config.SQLiteDialect